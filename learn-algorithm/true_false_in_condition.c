#include <stdio.h>
int main()
{
	
	int a = 10;
	if(a)// greater than and equal 1 is true
	printf("True_Ok\n");
	
	a = 0;
	
	if(!a)// it means if a is false the code inside condition will execute. In computer 0 = false.
	printf("False_Ok");
	
	return 0;
}
